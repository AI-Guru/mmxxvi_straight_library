[project]
name = "straight-library-tests"
version = "0.1.0"
description = "Integration tests for Straight Library"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "requests>=2.31",
]
